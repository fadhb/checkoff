<%= form_for(@task_item) do |f| %>
  <% if @task_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_item.errors.count, "error") %> prohibited this task_item from being saved:</h2>

      <ul>
      <% @task_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.text_field :task_id %>
  </div>
  <div class="field">
    <%= f.label :task_list_id %><br>
    <%= f.text_field :task_list_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
